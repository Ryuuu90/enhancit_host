// const mongoose = require('mongoose');
// require('dotenv').config();

// const mainConnection = mongoose.createConnection(process.env.DATABASE);

// const analysisConnection = mongoose.createConnection(process.env.MONGO_URI);

// mainConnection.on('connected', () => console.log('Main DB connected'));
// analysisConnection.on('connected', () => console.log('Analysis DB connected'));

// module.exports = {
//   mainConnection,
//   analysisConnection,
// };
